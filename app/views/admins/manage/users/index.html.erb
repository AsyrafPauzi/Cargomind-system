
    <section class="content-header">
      <div class="container-fluid">
        <div class="row mb-2">
          <div class="col-sm-6">
            <h1>Users</h1>
          </div>
          <div class="col-sm-6">
            <ol class="breadcrumb float-sm-right">
              <li class="breadcrumb-item"><%= link_to "Dashboard" , admins_path %></li>
              <li class="breadcrumb-item active">Users</li>
            </ol>
          </div>
        </div>
      </div><!-- /.container-fluid -->
    </section>

     <!-- Main content -->
    <section class="content">
      <%= link_to "Create User" , new_admins_manage_user_path, class:"btn btn-default my-3" %> 
      <!-- Default box -->
      <div class="card">
            <div class="card-header">
              <h3 class="card-title">Listing Users</h3>
            </div>
            <!-- /.card-header -->
            <div class="card-body">
              <table id="table3" class="table table-striped table-bordered" style="width:100%">
                            <thead>
                                <tr>
                                    <th>No. </th>
                                    <th>Date Create</th>
                                    <th>Email</th>
                                    <th>Name</th>
                                    <th>No Telephone</th>
                                    <th>Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% @user.each_with_index do |users, index|%>
                                    <tr>
                                        <td><%= index + 1 %></td>
                                        <td><%= users.created_at.strftime("%Y-%m-%d") %></td>
                                        <td><%= users.email %></td>
                                        <td><%= users.name %></td>
                                        <td><%= users.no_tel %></td>
                                        <td> 
                                        <p class="buttons">
                                            <%= link_to '<span>View</span>'.html_safe,  admins_manage_user_path(users.id), data: {turbolinks: false}, class: 'btn btn-primary'%>
                                            <%= link_to '<span>Delete</span>'.html_safe,  admins_manage_user_path(users.id), :method => :delete, data: { confirm: 'Are you sure you want to delete this User?' }, class: 'btn btn-danger'%>
                                        </p>
                                        </td>
                                    </tr>
                                <% end %>
                            </tbody>
              </table>
            </div>
            <!-- /.card-body -->
          </div>
      <!-- /.card -->

    </section>

<script>
$(document).ready(function() {
    $('#table3').DataTable();
} );
</script>