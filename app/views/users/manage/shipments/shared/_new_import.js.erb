
        $('#date_import').on("change", function () {
            $("#quotation_import_date").val($(this).val());
        });

        $('#to_import').on("change", function () {
            $("#quotation_import_to").val($(this).val());
        });

        $('#attn_import').on("change", function () {
            $("#quotation_import_attn").val($(this).val());
        });

        $('#booking_no_import').on("change", function () {
            $("#quotation_import_booking_no").val($(this).val());
        });

        $('#shipper_import').on("change", function () {
            $("#quotation_import_shipper").val($(this).val());
        });

        $('#consignee_import').on("change", function () {
            $("#quotation_import_consignee").val($(this).val());
        });

        $('#port_of_loading_import').on("change", function () {
            $("#quotation_import_port_of_loading").val($(this).val());
        });

        $('#port_of_discharge_import').on("change", function () {
            $("#quotation_import_port_of_discharge").val($(this).val());
        });

        $('#final_destination_import').on("change", function () {
            $("#quotation_import_final_destination").val($(this).val());
        });

        $('#cargo_type_import').on("change", function () {
            $("#quotation_import_cargo_type").val($(this).val());
        });

        $('#weight_type_import').on("change", function () {
            $("#quotation_import_weight_type").val($(this).val());
        });

        $('#fcl_import').on("change", function () {
            $("#quotation_import_weight_fcl").val($(this).val());
        });

        $('#lcl_import').on("change", function () {
            $("#quotation_import_weight_lcl").val($(this).val());
        });

        $('#air_import').on("change", function () {
            $("#quotation_import_weight_air").val($(this).val());
        });

        $('#commodity_import').on("change", function () {
            $("#quotation_import_commodity").val($(this).val());
        });

        
        
        
        
        
        
        
        
        
        
        
        
        